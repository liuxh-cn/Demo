'''
np.random.
    rand(d0, d1, ..)
    randn(d0, d1, ..)
    randint(low, high, size=(d0, d1, ..))
np.ones/zeros(shape=(d0, d1, ..))
np.linspace()
np.arange()
'''
import numpy as np

r = np.arange(0, 10, 2)
np.random.randint(1,1,1)
print(r)